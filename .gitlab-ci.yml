stages:
  - build
  - test

build:
  stage: build
  image: golang:latest
  script:
    - docker --version
    - docker build -f Dockerfile .

test:
  stage: test
  image: golang:latest
  script:
    - go test ./... -v
  only:
    - main
