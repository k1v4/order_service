stages:
  - build
  - test

build:
  stage: build
  image: docker:latest
  services:
    - docker:dind
  script:
    - ls -l ./conf/nginx.conf
    - docker compose up -d

test:
  stage: test
  image: golang:latest
  services:
    - docker:dind 
  script:
    - go test -v