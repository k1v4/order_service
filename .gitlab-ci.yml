stages:
  - build
  - test

build:
  stage: build
  image: docker:latest
  script:
    - docker -v
    - docker compose build

test:
  stage: test
  image: golang:latest
  script:
    - go test ./cmd/main -v