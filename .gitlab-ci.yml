stages:
  - build
  - test

build:
  stage: build
  image: docker:latest
  script:
    - docker --version
    - docker info
    - docker compose .

test:
  stage: test
  image: golang:latest
  script:
    - go test ./... -v
  only:
    - main
