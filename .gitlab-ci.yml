stages:
  - build
  - test

variables:
  DOCKER_IMAGE: 'docker:latest'

build:
  stage: build
  image: $DOCKER_IMAGE
  script:
    - docker compose up --build -d
  artifacts:
    paths:
      - docker-compose.yml

test:
  stage: test
  image: golang:latest
  script:
    - go test ./... -v
  only:
    - main
