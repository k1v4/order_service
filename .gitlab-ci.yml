stages:
  - build
  - test

build:
  stage: build
  image: docker:27-rc-dind
  script:
    - docker --version
    - docker build .

test:
  stage: test
  image: golang:latest
  script:
    - go test ./... -v
  only:
    - main
