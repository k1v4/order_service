stages:
  - build
  - test

build:
  stage: build
  image: docker:27-rc-dind
  services:
    - docker:27-dind
  script:
    - docker --version
    - docker info
    - docker build .

test:
  stage: test
  image: golang:latest
  script:
    - go test ./... -v
  only:
    - main
