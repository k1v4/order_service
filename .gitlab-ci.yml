stages:
  - build
  - test

build:
  stage: build
  services:
      - docker:dind
  script:
    - docker compose up --build -d
  artifacts:
    paths:
      - docker-compose.yml

test:
  stage: test
  script:
    - go test ./... -v
  only:
    - main